#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks the value of character
 *
 * @c - variable digit
 *
 * Return: Always 0.
 */
int _isdigit(int c);
{
	char c;

	c = 'o';
	if (c <= 9)
	{
	return (1);
	}
	c = 'a';
	else
	{
	return (0);
	}
}
